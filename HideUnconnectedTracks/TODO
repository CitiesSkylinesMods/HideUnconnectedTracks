- if info.connectGroup == DOUBLE and info.connectGroup == (None or DOUBLE):
     NetInfo.Node.connectGroup == None is same as NetInfo.Node.connectGroup == DOUBLE   

- benchmark loading
    - why does it take 10 sec to generate double track meshes? (code ready)

- test/support bidriractional single tracks and one directional double tracks.


change log:
- multi nodeinfo support.
- track-famlilies.txt is initialized by default tracks.
